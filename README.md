TCP and UDP Client-Server Communication using Socket Programming

Instruction to run:
- Run `python [udp/tcp].py client` on a terminal window
- Run `python [udp/tcp].py server` on another terminal window
